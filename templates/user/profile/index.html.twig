{% extends 'plongee_base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
<div class="profile-page">
    <!-- En-tête -->
    <div class="profile-card">
        <div class="profile-card-header">
            <div class="profile-header">
                <div class="profile-header-left">
                    {% if user.avatarFile %}
                        <img src="{{ asset('uploads/avatars/' ~ user.avatarFile) }}"
                             alt="Avatar {{ user.fullName }}"
                             class="profile-avatar">
                    {% else %}
                        <div class="profile-avatar-placeholder">
                            <span>{{ user.fullName|slice(0, 1)|upper }}</span>
                        </div>
                    {% endif %}
                    <div class="profile-user-info">
                        <h1 class="profile-user-name">{{ user.fullName }}</h1>
                        <p class="profile-user-email">{{ user.email }}</p>
                    </div>
                </div>

                <!-- Upload avatar -->
                <div class="profile-avatar-upload">
                    <form method="POST" action="{{ path('user_profile_upload_avatar') }}" enctype="multipart/form-data" id="avatar-form">
                        <input type="hidden" name="_token" value="{{ csrf_token('upload_avatar') }}">
                        <label for="avatar-input" class="profile-avatar-label">
                            <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                            </svg>
                            Changer l'avatar
                        </label>
                        <input type="file" id="avatar-input" name="avatar" accept="image/*" class="hidden" onchange="document.getElementById('avatar-form').submit()">
                    </form>
                    {% if user.avatarFile %}
                        <form method="POST" action="{{ path('user_profile_delete_avatar') }}">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete_avatar') }}">
                            <button type="submit" class="profile-delete-avatar" onclick="return confirm('Supprimer votre avatar ?')">
                                Supprimer l'avatar
                            </button>
                        </form>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Informations de base -->
        <div class="profile-card-body">
            <div class="profile-info-grid">
                <div class="profile-info-item">
                    <dt>Statut</dt>
                    <dd>
                        {% if user.status == 'approved' %}
                            <span class="profile-badge profile-badge-green">✓ Approuvé</span>
                        {% elseif user.status == 'pending' %}
                            <span class="profile-badge profile-badge-yellow">⏳ En attente</span>
                        {% else %}
                            <span class="profile-badge profile-badge-red">❌ {{ user.status|title }}</span>
                        {% endif %}
                    </dd>
                </div>
                <div class="profile-info-item">
                    <dt>Membre depuis</dt>
                    <dd>{{ user.createdAt|date('d/m/Y') }}</dd>
                </div>
                <div class="profile-info-item">
                    <dt>Dernière mise à jour</dt>
                    <dd>
                        {% if user.updatedAt %}
                            {{ user.updatedAt|date('d/m/Y') }}
                        {% else %}
                            Non modifié
                        {% endif %}
                    </dd>
                </div>
            </div>
        </div>
    </div>

    <!-- Alerte compte en attente -->
    {% if user.status == 'pending' %}
    <div class="profile-alert profile-alert-warning">
        <div class="profile-alert-inner">
            <div class="profile-alert-icon">
                <svg width="24" height="24" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                </svg>
            </div>
            <div class="profile-alert-content">
                <h3 class="profile-alert-title">Compte en attente de validation</h3>
                <div class="profile-alert-text">
                    <p>Votre compte est en attente d'approbation par un administrateur du club. Vous pourrez vous inscrire aux plongées dès que votre compte sera validé.</p>
                    <p class="profile-alert-date">Membre depuis le {{ user.createdAt|date('d/m/Y à H:i') }}</p>
                    <form method="POST" action="{{ path('user_profile_remind_admins') }}" style="display: inline-block;">
                        <input type="hidden" name="_token" value="{{ csrf_token('remind_admins') }}">
                        <button type="submit" class="profile-btn profile-btn-warning">
                            <svg width="20" height="20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                            </svg>
                            Relancer les administrateurs
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    {% endif %}

    <!-- Niveaux et Activités -->
    <div class="profile-card">
        <div class="profile-card-header">
            <h2 class="profile-card-title">🏊 Activités et Niveaux</h2>
            <p class="profile-card-subtitle">Définissez vos activités et niveaux pour accéder aux événements correspondants.</p>
        </div>

        <div class="profile-card-body">
            <form method="POST" action="{{ path('user_profile_activities') }}" class="profile-form">
                <input type="hidden" name="_token" value="{{ csrf_token('profile_activities') }}">

                <!-- Activités pratiquées -->
                <div class="profile-form-group">
                    <label>Mes activités</label>
                    <div class="profile-checkbox-group">
                        <label class="profile-checkbox-label">
                            <input type="checkbox" name="is_diver" value="1" {{ user.isDiver ? 'checked' : '' }}
                                   onchange="document.getElementById('diving_level_section').style.display = this.checked ? 'block' : 'none'"
                                   class="profile-checkbox">
                            <span class="profile-checkbox-text">🤿 Je pratique la plongée bouteille</span>
                        </label>
                        <label class="profile-checkbox-label">
                            <input type="checkbox" name="is_freediver" value="1" {{ user.isFreediver ? 'checked' : '' }}
                                   onchange="document.getElementById('freediving_level_section').style.display = this.checked ? 'block' : 'none'"
                                   class="profile-checkbox">
                            <span class="profile-checkbox-text">🌊 Je pratique l'apnée</span>
                        </label>
                        <label class="profile-checkbox-label">
                            <input type="checkbox" name="is_pilot" value="1" {{ user.isPilot ? 'checked' : '' }}
                                   class="profile-checkbox">
                            <span class="profile-checkbox-text">⛵ Je suis pilote de bateau</span>
                        </label>
                        <label class="profile-checkbox-label">
                            <input type="checkbox" name="is_lifeguard" value="1" {{ user.isLifeguard ? 'checked' : '' }}
                                   class="profile-checkbox">
                            <span class="profile-checkbox-text">🏊 Je suis surveillant de baignade</span>
                        </label>
                    </div>
                </div>

                <!-- Niveau de plongée -->
                <div id="diving_level_section" style="display: {{ user.isDiver ? 'block' : 'none' }}">
                    <div class="profile-form-group">
                        <label for="diving_level_id">Niveau de plongée bouteille</label>
                        <select name="diving_level_id" id="diving_level_id" class="profile-select profile-input-medium">
                            <option value="">-- Sélectionner mon niveau --</option>
                            {% for level in divingLevels %}
                                <option value="{{ level.id }}"
                                        {{ user.highestDivingLevel and user.highestDivingLevel.id == level.id ? 'selected' : '' }}>
                                    {{ level.name }}
                                    {% if level.description %} - {{ level.description }}{% endif %}
                                </option>
                            {% endfor %}
                        </select>
                    </div>
                </div>

                <!-- Niveau d'apnée -->
                <div id="freediving_level_section" style="display: {{ user.isFreediver ? 'block' : 'none' }}">
                    <div class="profile-form-group">
                        <label for="freediving_level_id">Niveau d'apnée</label>
                        <select name="freediving_level_id" id="freediving_level_id" class="profile-select profile-input-medium">
                            <option value="">-- Sélectionner mon niveau --</option>
                            {% for level in freedivingLevels %}
                                <option value="{{ level.id }}"
                                        {{ user.highestFreedivingLevel and user.highestFreedivingLevel.id == level.id ? 'selected' : '' }}>
                                    {{ level.name }}
                                    {% if level.description %} - {{ level.description }}{% endif %}
                                </option>
                            {% endfor %}
                        </select>
                    </div>
                </div>

                <div class="profile-form-footer">
                    <button type="submit" class="profile-btn profile-btn-primary">
                        <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                        </svg>
                        Enregistrer
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Informations personnelles -->
    <div class="profile-card">
        <div class="profile-card-header">
            <h2 class="profile-card-title">👤 Informations Personnelles</h2>
            <p class="profile-card-subtitle">Ces informations sont visibles uniquement par les Directeurs de Plongée.</p>
        </div>

        <div class="profile-card-body">
            <form method="POST" action="{{ path('user_profile_personal_info') }}" class="profile-form">
                <input type="hidden" name="_token" value="{{ csrf_token('personal_info') }}">

                <!-- Téléphone mobile -->
                <div class="profile-form-group">
                    <label for="phone_number">📱 Numéro de téléphone mobile</label>
                    <input type="tel"
                           name="phone_number"
                           id="phone_number"
                           value="{{ user.phoneNumber ?? '' }}"
                           placeholder="Ex: 06 12 34 56 78"
                           class="profile-input profile-input-medium">
                    <p class="profile-hint">Pour vous contacter en cas d'urgence</p>
                </div>

                <!-- Date de naissance -->
                <div class="profile-form-group">
                    <label for="date_of_birth">🎂 Date de naissance</label>
                    <input type="date"
                           name="date_of_birth"
                           id="date_of_birth"
                           value="{{ user.dateOfBirth ? user.dateOfBirth|date('Y-m-d') : '' }}"
                           class="profile-input profile-input-small">
                    <p class="profile-hint">Utile pour les assurances et validité des certificats</p>
                </div>

                <!-- Adresse postale -->
                <div class="profile-form-group">
                    <label for="address">🏠 Adresse postale</label>
                    <textarea name="address"
                              id="address"
                              rows="3"
                              placeholder="Numéro et rue&#10;Code postal et ville"
                              class="profile-textarea">{{ user.address ?? '' }}</textarea>
                    <p class="profile-hint">Pour l'envoi de documents administratifs</p>
                </div>

                <div class="profile-form-footer profile-form-footer-between">
                    <div class="profile-form-info">
                        <p><strong>🔒 Confidentialité :</strong></p>
                        <p>Ces informations ne sont visibles que par les DP et ne sont jamais partagées.</p>
                    </div>

                    <button type="submit" class="profile-btn profile-btn-primary">
                        <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                        </svg>
                        Enregistrer
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Certificat Médical (CACI) -->
    <div class="profile-card">
        <div class="profile-card-header">
            <h2 class="profile-card-title">🏥 Certificat Médical (CACI)</h2>
            <p class="profile-card-subtitle">Téléchargez votre certificat médical pour validation par un référent CACI.</p>
        </div>

        <div class="profile-card-body">
            <!-- Statut actuel du CACI -->
            {% if caciStatus.status == 'valid' %}
                <div class="profile-status-box profile-status-box-green">
                    <div class="profile-status-icon profile-status-icon-green">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-green">CACI validé</h3>
                        <div class="profile-status-text profile-status-text-green">
                            <p>Date d'expiration : {{ caciStatus.certificate.expiryDate|date('d/m/Y') }}</p>
                            {% if caciStatus.certificate.validatedAt %}
                                <p>Validé le {{ caciStatus.certificate.validatedAt|date('d/m/Y') }}{% if caciStatus.certificate.validatedBy %} par {{ caciStatus.certificate.validatedBy.fullName }}{% endif %}</p>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% elseif caciStatus.status == 'pending' %}
                <div class="profile-status-box profile-status-box-yellow">
                    <div class="profile-status-icon profile-status-icon-yellow">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-yellow">En attente de validation</h3>
                        <div class="profile-status-text profile-status-text-yellow">
                            <p>Date d'expiration : {{ caciStatus.certificate.expiryDate|date('d/m/Y') }}</p>
                            <p>Ajouté le {{ caciStatus.certificate.uploadedAt|date('d/m/Y à H:i') }}</p>
                            <p><em>Vous pouvez vous inscrire aux événements en attendant.</em></p>
                        </div>
                    </div>
                </div>
            {% elseif caciStatus.status == 'expired' %}
                <div class="profile-status-box profile-status-box-red">
                    <div class="profile-status-icon profile-status-icon-red">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-red">CACI expiré</h3>
                        <div class="profile-status-text profile-status-text-red">
                            <p>Expiré depuis le : {{ caciStatus.certificate.expiryDate|date('d/m/Y') }}</p>
                            <p><strong>Vous ne pouvez pas vous inscrire aux événements.</strong></p>
                        </div>
                    </div>
                </div>
            {% elseif caciStatus.status == 'rejected' %}
                <div class="profile-status-box profile-status-box-red">
                    <div class="profile-status-icon profile-status-icon-red">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-red">CACI rejeté</h3>
                        <div class="profile-status-text profile-status-text-red">
                            <p><strong>Motif :</strong> {{ caciStatus.certificate.rejectionReason }}</p>
                            <p>Veuillez soumettre un nouveau certificat.</p>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="profile-status-box profile-status-box-yellow">
                    <div class="profile-status-icon profile-status-icon-yellow">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-yellow">Aucun CACI</h3>
                        <p class="profile-status-text profile-status-text-yellow">Téléchargez votre certificat médical pour pouvoir participer aux plongées.</p>
                    </div>
                </div>
            {% endif %}

            <!-- Actions sur CACI existant -->
            {% if caciStatus.certificate %}
            <div style="margin: 1rem 0; display: flex; gap: 1rem; flex-wrap: wrap;">
                <a href="{{ path('member_caci_view', {id: caciStatus.certificate.id}) }}"
                   target="_blank" class="profile-btn profile-btn-secondary">
                    <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                    </svg>
                    Voir mon CACI
                </a>
                <a href="{{ path('member_caci_download', {id: caciStatus.certificate.id}) }}"
                   class="profile-btn profile-btn-outline">
                    <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
                    </svg>
                    Télécharger
                </a>
                {% if caciStatus.certificate.isPending %}
                <form method="POST" action="{{ path('member_caci_delete', {id: caciStatus.certificate.id}) }}" style="display: inline;">
                    <input type="hidden" name="_token" value="{{ csrf_token('caci_delete_' ~ caciStatus.certificate.id) }}">
                    <button type="submit" class="profile-btn profile-btn-danger"
                            onclick="return confirm('Supprimer ce CACI ?')">
                        <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                        </svg>
                        Supprimer
                    </button>
                </form>
                {% endif %}
            </div>
            {% endif %}

            <!-- Formulaire d'upload -->
            <form method="POST" action="{{ path('member_caci_upload') }}" enctype="multipart/form-data" class="profile-form" style="margin-top: 1.5rem; border-top: 1px solid #e5e7eb; padding-top: 1.5rem;">
                <h3 style="margin-bottom: 1rem; font-size: 1rem; font-weight: 600;">
                    {% if caciStatus.certificate %}Remplacer mon CACI{% else %}Ajouter mon CACI{% endif %}
                </h3>
                <input type="hidden" name="_token" value="{{ csrf_token('caci_upload') }}">

                <div class="profile-form-group">
                    <label for="caci_file">Fichier CACI (PDF, JPG, PNG, WebP - max 5 Mo)</label>
                    <input type="file" id="caci_file" name="caci_file"
                           accept=".pdf,.jpg,.jpeg,.png,.webp,application/pdf,image/jpeg,image/png,image/webp"
                           required class="profile-input-file">
                </div>

                <div class="profile-form-group">
                    <label for="expiry_date">Date d'expiration</label>
                    <input type="date" id="expiry_date" name="expiry_date"
                           min="{{ 'now'|date('Y-m-d') }}"
                           required class="profile-input profile-input-small">
                    <p class="profile-hint">La date d'expiration est généralement indiquée sur le certificat (1 an après la date de délivrance).</p>
                </div>

                <!-- RGPD Consent -->
                <div class="profile-form-group">
                    <label class="profile-checkbox-label">
                        <input type="checkbox" name="consent" value="1" required class="profile-checkbox">
                        <span class="profile-checkbox-text">
                            J'accepte que mon certificat médical soit stocké de manière sécurisée et consulté uniquement par les référents CACI (pour validation) et les directeurs de plongée des événements auxquels je suis inscrit.
                        </span>
                    </label>
                    <p class="profile-hint">
                        Conformément au RGPD, vos données de santé sont chiffrées et seront automatiquement supprimées 1 an après l'expiration du certificat.
                    </p>
                </div>

                <div class="profile-form-footer profile-form-footer-between">
                    <div class="profile-form-info">
                        <p><strong>Comment ça marche :</strong></p>
                        <ul>
                            <li>Téléchargez un scan de votre CACI</li>
                            <li>Un référent CACI validera votre certificat</li>
                            <li>En attendant, vous pouvez vous inscrire aux événements</li>
                            <li>Votre certificat est chiffré (conformité RGPD)</li>
                        </ul>
                    </div>

                    <button type="submit" class="profile-btn profile-btn-primary">
                        <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
                        </svg>
                        {% if caciStatus.certificate %}Remplacer mon CACI{% else %}Télécharger mon CACI{% endif %}
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Cotisation Club -->
    <div class="profile-card">
        <div class="profile-card-header">
            <h2 class="profile-card-title">💳 Cotisation Club</h2>
            <p class="profile-card-subtitle">Votre cotisation pour la saison en cours.</p>
        </div>

        <div class="profile-card-body">
            {% set membershipStatus = user.membershipStatus %}

            {% if membershipStatus == 'valid' %}
                <div class="profile-status-box profile-status-box-green">
                    <div class="profile-status-icon profile-status-icon-green">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-green">✓ Cotisation à jour</h3>
                        <div class="profile-status-text profile-status-text-green">
                            <p>Saison : {{ user.membershipSeason }}</p>
                            <p>Montant : {{ user.membershipAmount }} €</p>
                            <p>Payée le {{ user.membershipPaidAt|date('d/m/Y') }}</p>
                            {% if user.membershipValidatedBy %}
                                <p>Enregistrée par {{ user.membershipValidatedBy.fullName }}</p>
                            {% endif %}
                        </div>
                    </div>
                </div>
            {% elseif membershipStatus == 'expired' %}
                <div class="profile-status-box profile-status-box-red">
                    <div class="profile-status-icon profile-status-icon-red">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-red">⚠️ Cotisation expirée</h3>
                        <div class="profile-status-text profile-status-text-red">
                            <p>Dernière cotisation : {{ user.membershipSeason }}</p>
                            <p><strong>Vous devez renouveler votre cotisation pour la saison {{ currentSeason }}.</strong></p>
                            <p>Contactez le trésorier ou un Directeur de Plongée.</p>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="profile-status-box profile-status-box-yellow">
                    <div class="profile-status-icon profile-status-icon-yellow">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="profile-status-content">
                        <h3 class="profile-status-title profile-status-title-yellow">Cotisation non réglée</h3>
                        <div class="profile-status-text profile-status-text-yellow">
                            <p>Vous n'avez pas encore réglé votre cotisation pour la saison {{ currentSeason }}.</p>
                            <p><strong>Vous ne pouvez pas vous inscrire aux événements.</strong></p>
                            <p>Contactez le trésorier ou un Directeur de Plongée pour régler votre cotisation.</p>
                        </div>
                    </div>
                </div>
            {% endif %}

            <div class="profile-info-box profile-info-box-blue">
                <div class="profile-info-box-inner">
                    <div class="profile-info-box-icon">
                        <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <p class="profile-info-box-text">
                        La cotisation club est gérée par le trésorier ou les Directeurs de Plongée. Une fois votre paiement reçu, votre cotisation sera enregistrée automatiquement.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Licence FFESSM -->
    <div class="profile-card">
        <div class="profile-card-header">
            <h2 class="profile-card-title">🎫 Licence FFESSM</h2>
            <p class="profile-card-subtitle">Ajoutez votre justificatif de licence pour permettre aux DP de vérifier sa validité.</p>
        </div>

        <div class="profile-card-body">
            <form method="POST" action="{{ path('user_profile_licence') }}" enctype="multipart/form-data" class="profile-form">
                <input type="hidden" name="_token" value="{{ csrf_token('licence') }}">

                <!-- Statut actuel -->
                {% if user.licenceFile %}
                    <div class="profile-status-box {{ user.isLicenceValid() ? 'profile-status-box-green' : 'profile-status-box-red' }}">
                        <div class="profile-status-icon {{ user.isLicenceValid() ? 'profile-status-icon-green' : 'profile-status-icon-red' }}">
                            {% if user.isLicenceValid() %}
                                <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                                </svg>
                            {% else %}
                                <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                                </svg>
                            {% endif %}
                        </div>
                        <div class="profile-status-content">
                            <h3 class="profile-status-title {{ user.isLicenceValid() ? 'profile-status-title-green' : 'profile-status-title-red' }}">
                                {% if user.isLicenceValid() %}
                                    ✓ Licence valide
                                {% else %}
                                    ⚠️ Licence expirée
                                {% endif %}
                            </h3>
                            <div class="profile-status-text {{ user.isLicenceValid() ? 'profile-status-text-green' : 'profile-status-text-red' }}">
                                {% if user.licenceNumber %}
                                    <p>Numéro : {{ user.licenceNumber }}</p>
                                {% endif %}
                                <p>Date d'expiration : {{ user.licenceExpiry|date('d/m/Y') }}</p>
                                <p><a href="{{ asset('uploads/licences/' ~ user.licenceFile) }}" target="_blank">Voir le justificatif</a></p>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="profile-status-box profile-status-box-yellow">
                        <div class="profile-status-icon profile-status-icon-yellow">
                            <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                            </svg>
                        </div>
                        <div class="profile-status-content">
                            <h3 class="profile-status-title profile-status-title-yellow">Aucun justificatif de licence</h3>
                            <p class="profile-status-text profile-status-text-yellow">Vous devez fournir un justificatif de licence valide pour participer aux plongées.</p>
                        </div>
                    </div>
                {% endif %}

                <!-- Numéro de licence -->
                <div class="profile-form-group">
                    <label for="licence_number">Numéro de licence</label>
                    <input type="text"
                           name="licence_number"
                           id="licence_number"
                           value="{{ user.licenceNumber ?? '' }}"
                           placeholder="Ex: 123456789"
                           class="profile-input profile-input-medium">
                    <p class="profile-hint">Numéro visible sur votre carte de licence</p>
                </div>

                <!-- Upload du justificatif -->
                <div class="profile-form-group">
                    <label for="licence_file">{{ user.licenceFile ? 'Remplacer le justificatif' : 'Fichier du justificatif' }}</label>
                    <input type="file"
                           name="licence_file"
                           id="licence_file"
                           accept=".pdf,.jpg,.jpeg,.png"
                           class="profile-file-input">
                    <p class="profile-hint">Formats acceptés : PDF, JPG, PNG (max 5 Mo)</p>
                </div>

                <!-- Date d'expiration -->
                <div class="profile-form-group">
                    <label for="licence_expiry">Date d'expiration (optionnelle)</label>
                    <input type="date"
                           name="licence_expiry"
                           id="licence_expiry"
                           value="{{ user.licenceExpiry ? user.licenceExpiry|date('Y-m-d') : '' }}"
                           class="profile-input profile-input-small">
                    <p class="profile-hint">La licence FFESSM est généralement valide pour la saison (31 août)</p>
                </div>

                <div class="profile-form-footer profile-form-footer-between">
                    <div class="profile-form-info">
                        <p><strong>Informations importantes :</strong></p>
                        <ul>
                            <li>La licence doit être en cours de validité</li>
                            <li>Visible uniquement par les Directeurs de Plongée</li>
                            <li>Vous serez notifié 30 jours avant l'expiration</li>
                        </ul>
                    </div>

                    <button type="submit" class="profile-btn profile-btn-primary">
                        <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
                        </svg>
                        Enregistrer la licence
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Notifications Push -->
    <div class="profile-card">
        <div class="profile-card-header">
            <h2 class="profile-card-title">🔔 Notifications push</h2>
        </div>
        <div class="profile-card-body">
            <div id="push-notifications-section">
                <!-- Status des notifications -->
                <div id="push-status" class="profile-push-status">
                    <div>
                        <h3>Statut</h3>
                        <p id="push-status-text">Vérification...</p>
                    </div>
                    <button id="toggle-push" class="profile-btn profile-btn-outline">
                        <span id="toggle-push-text">Charger...</span>
                    </button>
                </div>

                <!-- Préférences -->
                <div id="push-preferences" class="profile-push-preferences hidden">
                    <h3>Préférences de notifications</h3>
                    <div>
                        <label class="profile-push-option">
                            <input type="checkbox" id="pref-registration" class="push-preference profile-checkbox">
                            <span class="profile-push-option-text">
                                <strong>Nouvelles inscriptions</strong>
                                <span>Notification quand un participant s'inscrit (DP uniquement)</span>
                            </span>
                        </label>
                        <label class="profile-push-option">
                            <input type="checkbox" id="pref-cancellation" class="push-preference profile-checkbox">
                            <span class="profile-push-option-text">
                                <strong>Désinscriptions</strong>
                                <span>Notification quand un participant se désinscrit (DP uniquement)</span>
                            </span>
                        </label>
                        <label class="profile-push-option">
                            <input type="checkbox" id="pref-waiting-list" class="push-preference profile-checkbox">
                            <span class="profile-push-option-text">
                                <strong>Place libérée</strong>
                                <span>Notification quand une place se libère en liste d'attente</span>
                            </span>
                        </label>
                        <label class="profile-push-option">
                            <input type="checkbox" id="pref-new-event" class="push-preference profile-checkbox">
                            <span class="profile-push-option-text">
                                <strong>Nouvelles plongées</strong>
                                <span>Notification quand une plongée est créée et que votre niveau permet d'y participer</span>
                            </span>
                        </label>
                        <label class="profile-push-option">
                            <input type="checkbox" id="pref-as-dp" class="push-preference profile-checkbox">
                            <span class="profile-push-option-text">
                                <strong>Notifications DP</strong>
                                <span>Toutes les notifications en tant que Directeur de Plongée</span>
                            </span>
                        </label>
                    </div>
                </div>

                <!-- Info -->
                <div class="profile-info-box profile-info-box-blue">
                    <div class="profile-info-box-inner">
                        <div class="profile-info-box-icon">
                            <svg width="20" height="20" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                            </svg>
                        </div>
                        <p class="profile-info-box-text">
                            Les notifications push vous permettent de recevoir des alertes en temps réel même quand le site n'est pas ouvert.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Attendre que window.pushManager soit initialisé
        function waitForPushManager() {
            return new Promise((resolve) => {
                if (window.pushManager) {
                    resolve();
                } else {
                    const checkInterval = setInterval(() => {
                        if (window.pushManager) {
                            clearInterval(checkInterval);
                            resolve();
                        }
                    }, 100);

                    // Timeout après 5 secondes
                    setTimeout(() => {
                        clearInterval(checkInterval);
                        resolve();
                    }, 5000);
                }
            });
        }

        document.addEventListener('DOMContentLoaded', async function() {
            const statusText = document.getElementById('push-status-text');
            const toggleButton = document.getElementById('toggle-push');
            const toggleButtonText = document.getElementById('toggle-push-text');
            const preferencesSection = document.getElementById('push-preferences');

            try {
                // Vérifier le support des notifications
                if (!('serviceWorker' in navigator) || !('PushManager' in window)) {
                    statusText.textContent = '❌ Notifications non supportées par votre navigateur';
                    toggleButton.style.display = 'none';
                    return;
                }

                // Attendre que pushManager soit prêt
                statusText.textContent = 'Initialisation...';
                await waitForPushManager();

                if (!window.pushManager) {
                    statusText.textContent = '⚠️ Service non disponible';
                    toggleButtonText.textContent = 'Réessayer';
                    toggleButton.addEventListener('click', () => location.reload());
                    return;
                }

                async function updateStatus() {
                    try {
                        const isSubscribed = await window.pushManager.isSubscribed();
                        if (isSubscribed) {
                            statusText.textContent = '✅ Notifications activées';
                            statusText.style.color = '#16a34a';
                            statusText.style.fontWeight = '500';
                            toggleButtonText.textContent = 'Désactiver';
                            toggleButton.className = 'profile-btn profile-btn-outline profile-btn-outline-red';
                            preferencesSection.classList.remove('hidden');
                            await loadPreferences();
                        } else {
                            statusText.textContent = '❌ Notifications désactivées';
                            statusText.style.color = '#6b7280';
                            toggleButtonText.textContent = 'Activer';
                            toggleButton.className = 'profile-btn profile-btn-outline profile-btn-outline-green';
                            preferencesSection.classList.add('hidden');
                        }
                    } catch (error) {
                        console.error('Error updating status:', error);
                        statusText.textContent = '⚠️ Erreur de vérification';
                        statusText.style.color = '#dc2626';
                    }
                }

            async function loadPreferences() {
                try {
                    const response = await fetch('/api/push/subscriptions');
                    const subscriptions = await response.json();
                    if (subscriptions.length > 0) {
                        const prefs = subscriptions[0].preferences;
                        document.getElementById('pref-registration').checked = prefs.notifyEventRegistration;
                        document.getElementById('pref-cancellation').checked = prefs.notifyEventCancellation;
                        document.getElementById('pref-waiting-list').checked = prefs.notifyWaitingListPromotion;
                        document.getElementById('pref-new-event').checked = prefs.notifyNewEvent;
                        document.getElementById('pref-as-dp').checked = prefs.notifyAsDP;
                    }
                } catch (error) {
                    console.error('Error loading preferences:', error);
                }
            }

            toggleButton.addEventListener('click', async function() {
                toggleButton.disabled = true;
                const isSubscribed = await window.pushManager.isSubscribed();
                if (isSubscribed) {
                    await window.pushManager.unsubscribe();
                } else {
                    const result = await window.pushManager.subscribe();
                    if (result.reason === 'permission_denied') {
                        alert('Autorisez les notifications dans votre navigateur pour activer cette fonctionnalité.');
                    }
                }
                await updateStatus();
                toggleButton.disabled = false;
            });

                document.querySelectorAll('.push-preference').forEach(checkbox => {
                    checkbox.addEventListener('change', async function() {
                        const preferences = {
                            notifyEventRegistration: document.getElementById('pref-registration').checked,
                            notifyEventCancellation: document.getElementById('pref-cancellation').checked,
                            notifyWaitingListPromotion: document.getElementById('pref-waiting-list').checked,
                            notifyNewEvent: document.getElementById('pref-new-event').checked,
                            notifyAsDP: document.getElementById('pref-as-dp').checked
                        };
                        const result = await window.pushManager.updatePreferences(preferences);
                        if (result.success) {
                            const originalText = statusText.textContent;
                            statusText.textContent = '✅ Préférences enregistrées !';
                            setTimeout(() => { statusText.textContent = originalText; }, 2000);
                        }
                    });
                });

                await updateStatus();
            } catch (error) {
                console.error('Push notification initialization error:', error);
                statusText.textContent = '⚠️ Erreur d\'initialisation';
                statusText.style.color = '#dc2626';
                toggleButtonText.textContent = 'Réessayer';
                toggleButton.addEventListener('click', () => location.reload());
            }
        });
    </script>
</div>
{% endblock %}
